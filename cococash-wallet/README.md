# cococash-wallet

Wallet microservice for CocoCash.

## Responsibilities
- Account creation
- Balance management
- Transfer processing with transactional consistency

## Run locally

1. Configure `.env` with PostgreSQL credentials.
2. Install dependencies:

   npm install

3. Start in development mode:

   npm run dev

Service runs on port 3000.